# RaspberryPi dotfiles

:wrench: dotfiles for RaspberryPi OS

## Installation

```shell
ssh <user>@<rasperrypi>
git clone https://github.com/devmatteini/raspberrypi-dotfiles ~/dotfiles && dotfiles
./bootstrap.sh
```
